import React, { useState } from "react";
import { Text, View, StyleSheet, Image, Alert, TouchableOpacity, ScrollView, FlatList, Pressable, Dimensions } from 'react-native';
import { Icon } from "react-native-elements";

export default function Gallery_Screen(){
    return(
       <View>
        <Text style={{color: 'black', justifyContent: 'center', alignItems: 'center'}}> This is the galleryScren </Text>
       </View>
    )
}

const styles = StyleSheet.create({
})